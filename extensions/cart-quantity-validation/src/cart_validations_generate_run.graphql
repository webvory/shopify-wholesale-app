query CartValidationsGenerateRunInput {
  shop {
    errorMessages: metafield(namespace: "wholesale_engine", key: "error_messages") {
      value
    }
  }
  buyerJourney {
    step
  }
  cart {
    buyerIdentity {
      customer {
        id
        numberOfOrders
        min_quantity: metafield(namespace: "wholesale_engine", key: "min_quantity") {
          value
        }
      }
    }
    lines {
      quantity
    }
  }
}
